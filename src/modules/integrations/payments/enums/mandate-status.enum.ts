export enum MandateStatus {
  CREATED = 'created',
  PENDING_AUTHENTICATION = 'pending_authentication',
  ACTIVE = 'active',
  PAUSED = 'paused',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired',
  REJECTED = 'rejected',
  FAILED = 'failed',
  TOKEN_MISSING_AFTER_CAPTURE = 'TOKEN_MISSING_AFTER_CAPTURE',
  
}
